# Archived Scripts

One-off debug and exploratory scripts from RC1 development. **Not wired into `package.json`.**

Kept for reference. For release verification use:

- `npm run verify:pages`
- `npm run verify:release`

| Script | Purpose |
|--------|---------|
| `debug-browser.mjs` | Browser debugging helper |
| `debug-orders-*.mjs` | Orders/kitchen loading investigations |
| `rc1-runtime-audit.mjs` | Early RC1 audit variant |
| `rc1-runtime-edge.mjs` | Edge-case runtime scenarios |
